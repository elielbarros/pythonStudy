v1 = 'a'
v2 = 'a'
# Python put the variable value into memory.
# If the value from two different variables are the same...
# Then Python use the same memory allocated those two variables.
# We can see by the variable id that is the id used to recognize the element 'a' on memory.
print(id(v1))
print(id(v2))

