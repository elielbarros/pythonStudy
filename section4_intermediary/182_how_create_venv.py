"""
# English
To create a Python Environment, we run the command on terminal:
python -m venv environment

Inside environment, we have 'Lib' that maintains all libraries installed. We also have 'Scripts' that maintains all
executable that will be used by our Virtual Environment

To see where is our python on Linux, execute the command on terminal:
which python

# Portuguese
Para criar um ambiente virtual python, precisamos rodar o seguinte comando no terminal:
python -m venv environment

Dentro da pasta environment, nos temos a pasta 'Lib' que contem todas as bibliotecas instaladas. Nos tambem temos os
'Scripts' que contem todos os executaveis usados pelo ambiente virtual.

Para ver onde estah o caminho do python, no linux, execute o comando no terminal:
which python
"""
