"""
# English
How install packages and libraries com pip
pypi.org - The Python Package Index (PyPI) is a repository of software for the Python programming language.

Command to install packages/libraries:
pip install package/library

Command to install a specific version package:
pip install package==1.0.1

It is also possible to use the command like this:
python -m pip install package/library

Command to uninstall package/library already installed:
pip uninstall package/library

Command to list packages installed:
pip freeze

Command to list available versions from package:
pip index versions package

Command to update a package installed to most recent version:
pip install package --upgrade

# Portuguese
Como instalar pacotes e bibliotecas com pip
pypi.org - The Python Package Index (PyPI) eh um repositorio de software para a linguagem de programacao Python.

Comando para instalar pacote/biblioteca:
pip install package/library

Comando para instalar um pacote de versao especifica:
pip install package==1.0.1

Tambem eh possivel usar o comando do PIP assim:
python -m pip install package/library

Comando para desinstalar pacote/biblioteca instalada:
pip uninstall package/library

Comando para listar pacotes instalados:
pip freeze

Comando para listar versoes disponveis de um pacote:
pip index versions package

Comando para atualizar um pacote para a versao mais recente:
pip install package --upgrade
"""