"""
# English

'self' is a convention to make a reference to class.

to define any method inside a class, we must use self.

'self' will represent the instance created by the class.

# Portuguese

'self' eh uma convensao que faz referencia a classe.

para definir qualquer metodo que seja, na classe, precisamos usar 'self'

'self' vai representar a instancia criada em relacao a classe.
"""